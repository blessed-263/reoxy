{
  "buildCommand": "node scripts/check-connection.mjs && npm run build",
  "functions": {
    "api/[...path].ts": {
      "includeFiles": "server/**",
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
