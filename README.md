# ReOxy / Micor

Receipt and flight-ticket generators.

## Run locally

```
npm install
npm run dev
```

Frontend env (Vercel): `VITE_API_URL`, `VITE_APP_URL`.  
API env (Railway): `DATABASE_URL`, `FRONTEND_URL`, `CORS_ORIGIN`.
